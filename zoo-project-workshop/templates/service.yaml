apiVersion: v1
kind: Service
metadata:
  name: {{ include "zoo-project-workshop.fullname" . }}
  labels:
    {{- include "zoo-project-workshop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zoo-project-workshop.selectorLabels" . | nindent 4 }}
